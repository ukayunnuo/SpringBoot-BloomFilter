server:
  port: 8080

spring:
  application:
    name: springboot-template
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  redis:
    redisson:
      file: classpath:redisson-config.yml
